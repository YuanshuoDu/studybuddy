name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  lint-test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: pairhub
          POSTGRES_PASSWORD: pairhub
          POSTGRES_DB: pairhub_test
        ports: ['5432:5432']
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
      - run: corepack enable
      - name: Install (server)
        working-directory: server
        run: pnpm install --frozen-lockfile
      - name: Lint
        working-directory: server
        run: pnpm lint
      - name: Test
        working-directory: server
        env:
          DATABASE_URL: postgres://pairhub:pairhub@localhost:5432/pairhub_test
        run: pnpm test
